#!/usr/bin/env bash

bspc monitor -a J
bspc desktop -f J

qjackctl &
export waitPID=$!

sleep 1

bspc monitor -a M
bspc desktop -f M

"$(dirname "$0")/bitwig-studio"

kill $waitPID

sleep 1

bspc desktop J -r
bspc desktop M -r
bspc desktop -f 1
